# Документация по системе активации ваучеров (POSA)

Данный документ описывает текущий функционал и логику работы системы активации ваучеров, включая интеграцию с Rokky и ручную обработку (Manual).

## 1. Обзор архитектуры

Система предназначена для продажи (Мерчанты) и активации (Клиенты) цифровых товаров.

**Основные компоненты:**
*   **Admin Panel**: Управление товарами, магазинами, Rokky SKU, обработка ручных заявок.
*   **Storefront (Клиентская часть)**: Брендированные страницы для активации ваучеров конечными пользователями (например `/roblox`, `/pubg`).
*   **Backend API**: Node.js (Express), Prisma ORM, PostgreSQL.

**Интеграции:**
*   **Eskiz**: SMS-шлюз для OTP авторизации.
*   **Rokky**: Поставщик цифровых ключей (API).
*   **Telegram**: Канал уведомлений для операторов (о ручных заявках).

---

## 2. Роли и Функционал

### 2.1. Администратор / Оператор
*   **Управление Продуктами**: Создание товаров, привязка к Магазинам и SKU.
*   **Управление Rokky SKU**: Ручное управление справочником SKU Rokky. *Автоматическое обновление цен не реализовано.*
*   **Обработка "Manual" заявок**: Получение уведомлений в Telegram и закрытие заявок в админке.
*   **Финансы**: Контроль балансов мерчантов и вендоров.

### 2.2. Клиент (Пользователь)
*   Вход на страницу активации по номеру телефона (SMS OTP).
*   Активация ваучера ("Manual" или "Rokky").
*   Просмотр истории.

---

## 3. Пошаговое описание процессов (Flows)

### 3.1. Активация ваучера (Клиент)

Процесс реализован в `storeController.js` -> `activateVoucher`.

**Шаг 1: Вход и Проверки**
1.  Клиент авторизуется по SMS.
2.  Вводит код ваучера.
3.  Система проверяет:
    *   Лимит попыток (Brute-force protection).
    *   Существование ваучера.
    *   Соответствие магазина (`Store mismatch`).
    *   Статус (`sold` или `pending`).
    *   Привязку к клиенту (если ваучер уже пытались активировать, он "занят" этим номером).

**Шаг 2: Обработка (Ветвление по типу продукта)**

**Вариант А: ROKKY (Автоматический)**
1.  Система отправляет запрос `createOrder` в Rokky API.
2.  **Успех:** Rokky возвращает ключ.
3.  Создается запись `RokkyOrder` (status: `COMPLETED`).
4.  Ваучер переходит в статус `activated`.
5.  Ключ мгновенно отображается клиенту.

**Вариант Б: MANUAL (Ручной)**
1.  Система создает `ManualActivationRequest` (status: `PENDING`).
2.  **Уведомление:** Система отправляет сообщение в Telegram (через `telegramService`) с деталями заявки.
3.  Клиенту возвращается сообщение: "Заявка принята. Ожидайте SMS".
4.  Оператор видит заявку в Telegram -> Выполняет услугу -> В админке ставит статус `COMPLETED` и вводит результат.
5.  Ваучер переходит в `activated`.

---

## 4. Технические детали

### 4.1. Обработка статуса 'Pending'
Статус `pending` означает "В процессе активации".
*   Для **Rokky**: Если запрос к API упал (или тайм-аут), повторная попытка клиента снова инициирует проверку API/повтор запроса. Если в базе Rokky заказ уже есть, ключ просто возвращается.
*   Для **Manual**: Если заявка уже создана (`ManualActivationRequest`), повторный ввод кода сообщит пользователю, что заявка в работе.

### 4.2. Безопасность
*   **SMS OTP**: Вход только по подтвержденному номеру.
*   **Привязка к клиенту (`OnlineVoucher`)**: Ваучер "прилипает" к первому клиенту, который ввел код.
*   **Rate Limiting**: Блокировка после 10 неудачных попыток.

---

## 5. Структура Базы Данных (Ключевые модели)

*   **Store**: Магазин (`slug`, `name`).
*   **Product**: Товар, связь с `Vendor` и `RokkySku`.
*   **Voucher**: Товарная единица.
*   **Vendor**: Поставщик (тип `ROKKY` или `MANUAL`).
*   **RokkyOrder**: Запись о транзакции в API Rokky.
*   **ManualActivationRequest**: Заявка на ручную активацию оператором.
*   **Client**: Пользователь Storefront.
